#include <stdio.h>

int main(void)
{
    int N, P;
    scanf("%d %d", &N, &P);
    for(int i = 1; i <= N; i++)
    {
        char string[255];
        scanf("%s", string);
    }
    printf("%d\n", P);
    return 0;
}
